image: freebsd/latest
packages:
  - go
sources:
  - https://github.com/mellium/xmlstream.git
tasks:
  - info: |
      go version
      go env
  - test: |
      cd xmlstream.git/
      go test -v -cover ./...
  - lint: |
      cd xmlstream.git/
      go vet ./...
      gofmt -s -l . && [ -z "$(gofmt -s -l .)" ]

      # This will modify go.mod, so do it last.
      go run golang.org/x/lint/golint ./...
